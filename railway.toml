[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py collectstatic --noinput && python manage.py migrate --noinput && python manage.py setup_roles && gunicorn config.wsgi --log-file -"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3