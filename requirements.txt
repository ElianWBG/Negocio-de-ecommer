# Django y extensiones del proyecto
Django==6.0.6
django-extensions==4.1
django-widget-tweaks==1.5.1

# Variables de entorno y conexión a base de datos
django-environ==0.11.2
psycopg[binary]==3.2.13

# Llamadas a la API de PayPhone
requests==2.32.3

# Exportación a Excel / PDF
openpyxl==3.1.5
reportlab==4.5.1

# Requerido por ImageField (foto de producto)
pillow==12.2.0

# Almacenamiento de imágenes en la nube (producción)
cloudinary==1.41.0
django-cloudinary-storage==0.3.0

# Servidor de producción y archivos estáticos — OBLIGATORIO para Railway
gunicorn==23.0.0
whitenoise==6.8.2
resend
